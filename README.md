# Projet Three.js: Rover Escape

## Description

## Installation

-Executer `npm install` à la racine du projet.
-Executer `npm run dev` pour lancer le serveur.
-Naviguer vers `http://localhost:5173/` dans votre navigateur.
-Jouez!
